name: ci

on:
  pull_request:
    branches:
      - master

permissions:
  contents: read

jobs:
  get-changed-images:
    name: get changed images
    runs-on: ubuntu-24.04
    if: ${{ !contains(github.event.pull_request.labels.*.name, 'release-please') }}
    outputs:
      images-to-build: ${{ steps.changed-files.outputs.all_changed_and_modified_files }}
    steps:
      - name: "Checkout code"
        uses: actions/checkout@de0fac2e4500dabe0009e67214ff5f5447ce83dd # v6.0.2
        with:
          persist-credentials: false

      - name: Get changed files in the images folder
        id: changed-files
        uses: tj-actions/changed-files@9426d40962ed5378910ee2e21d5f8c6fcbf2dd96 # v47.0.6
        with:
          path: images/
          files: "./**"
          dir_names_exclude_current_dir: true
          files_ignore: version.txt
          dir_names: true
          matrix: true

      - name: print
        env:
          CHANGED_FILES_LIST: "${{ steps.changed-files.outputs.all_changed_and_modified_files }}"
        run: |
          echo "${CHANGED_FILES_LIST}"

  build:
    uses: miracum/.github/.github/workflows/standard-build.yaml@6c54f95d52ffc2aaddb163da9680a2d63c6accd9 # v1.21.1
    needs:
      - get-changed-images
    permissions:
      contents: write
      id-token: write
      packages: write
      pull-requests: write
      actions: read
      security-events: write
    strategy:
      matrix:
        image: ${{ fromJSON(needs.get-changed-images.outputs.images-to-build) }}
    with:
      image: ghcr.io/${{ github.repository }}/${{ matrix.image }}
      build-context: images/${{ matrix.image }}
      enable-build-test-layer: false
      enable-upload-test-image: false
      enable-upload-image: false
      enable-trivy-scan: ${{ matrix.image != 'ml-on-fhir' }}
    secrets:
      github-token: ${{ secrets.GITHUB_TOKEN }}

  lint:
    uses: miracum/.github/.github/workflows/standard-lint.yaml@6c54f95d52ffc2aaddb163da9680a2d63c6accd9 # v1.21.1
    if: ${{ !contains(github.event.pull_request.labels.*.name, 'release-please') }}
    permissions:
      contents: read
      pull-requests: write
      issues: write
      security-events: write
      actions: read
    with:
      enable-validate-gradle-wrapper: false
      enable-codeql: false
      enable-verify-base-image-signature: false
    secrets:
      github-token: ${{ secrets.GITHUB_TOKEN }}
